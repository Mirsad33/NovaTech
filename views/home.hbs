  <div class="d-flex align-items-center justify-content-evenly flex-column h-100">
   <h1>Home</h1>
    </div>
        <div class="d-flex align-items-center justify-content-evenly flex-column h-100">
    
    {{#each posts as |post|}}
    <div class="post d-flex align-items-start justify-content-center flex-column bg-secondary rounded p-3"> 
        <h3 class="text-light">{{post.title}}</h3>
        <p class="text-dark-subtle">{{post.text}}</p>
        <p class="text-info">Created On: {{post.date_created}}</p>
    </div>
    {{/each}}


    {{#unless posts}}
    <p>Nothing has been posted yet 🫠 </p>
    {{/unless}}
    </div>